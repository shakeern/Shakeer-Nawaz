// Initialize ScrollReveal
const sr = ScrollReveal();

// Reveal the #projects section with a fade-in effect
sr.reveal('#projects', {
  duration: 1200,
  origin: 'bottom', // Coming from the bottom
  distance: '50px', // Distance moved while animating
  opacity: 0, // Start from invisible
  easing: 'ease-in-out',
  reset: true // Reset animation when scrolling back up
});
sr.reveal('#skills', {
    duration: 1200,
    origin: 'bottom', // Coming from the bottom
    distance: '50px', // Distance moved while animating
    opacity: 0, // Start from invisible
    easing: 'ease-in-out',
    reset: true // Reset animation when scrolling back up
  });
  sr.reveal('#interests', {
    duration: 1200,
    origin: 'bottom', // Coming from the bottom
    distance: '50px', // Distance moved while animating
    opacity: 0, // Start from invisible
    easing: 'ease-in-out',
    reset: true // Reset animation when scrolling back up
  });
  sr.reveal('#about-me', {
    duration: 1200,
    origin: 'bottom', // Coming from the bottom
    distance: '50px', // Distance moved while animating
    opacity: 0, // Start from invisible
    easing: 'ease-in-out',
    reset: true // Reset animation when scrolling back up
  });
  sr.reveal('#contact-me', {
    duration: 1200,
    origin: 'bottom', // Coming from the bottom
    distance: '50px', // Distance moved while animating
    opacity: 0, // Start from invisible
    easing: 'ease-in-out',
    reset: true // Reset animation when scrolling back up
  });

  
  
